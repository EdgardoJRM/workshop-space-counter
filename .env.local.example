# Upstash Redis (REST API) — create a database at https://console.upstash.com/
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Secret token for POST /api/admin/spaces (use a long random string in production)
ADMIN_TOKEN=
