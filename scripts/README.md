# Scripts

Utility scripts for local operations, deployment checks, and one-off maintenance.

- `*.mjs`: runnable Node scripts.
- `sql/`: manual SQL patches for environments where `prisma db push` is not available or not enough.

