import { PrintStation } from "@/components/staff/PrintStation";

export const metadata = {
  title: "Hernandez Pass — Estación de impresión",
};

export default function StaffPrintStationPage() {
  return <PrintStation />;
}
