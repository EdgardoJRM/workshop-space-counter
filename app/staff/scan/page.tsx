import { StaffScanner } from "@/components/staff/StaffScanner";

export const metadata = {
  title: "Hernandez Pass — Scanner",
};

export default function StaffScanPage() {
  return <StaffScanner />;
}
